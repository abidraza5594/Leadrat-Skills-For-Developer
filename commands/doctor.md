# doctor

Validates AI assistant health.

Checks include missing docs, missing AI configuration, invalid `.ai-dev-assistant` folder structure, and stale documentation based on the source fingerprint from the last `learn` run.

# learn

Scans the Angular project and generates markdown knowledge documents.

Detected areas include components, services, controllers, interfaces, enums, constants, utilities, pipes, directives, guards, interceptors, NgRx store files, routes, and API usage.

Generated sections are wrapped in managed markers so custom notes outside the generated section remain intact.
